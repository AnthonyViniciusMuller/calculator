import { ExpressionResultPipe } from './expression-result.pipe';

describe('ExpressionResultPipe', () => {
  it('create an instance', () => {
    const pipe = new ExpressionResultPipe();
    expect(pipe).toBeTruthy();
  });
});
